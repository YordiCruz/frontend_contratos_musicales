import { Component, OnInit, signal } from '@angular/core';
import { CategoriaComponent } from '../form/form';
import { PrimengModule } from '../../../../primeng/primeng-module';

@Component({
  selector: 'app-list-categorias',
  imports: [CategoriaComponent, PrimengModule],
  templateUrl: './list.html',
  styleUrls: ['./list.scss'],
})
// export class ListCategorias implements OnInit {
export class ListCategorias {

//   categorias = signal<CategoriaIntegrante[]>([]);
//   loading = signal(true);
//   visible = false;  // Controla la visibilidad del diálogo

//   constructor(private categoriasService: CategoriasIntegrantesService) {}

//   ngOnInit(): void {
//     this.loadCategorias();
//   }

//   loadCategorias() {
//     this.loading.set(true);
//     this.categoriasService.getAll().subscribe({
//       next: (data) => {
//         console.log('✅ Categorías recibidas:', data);
//         this.categorias.set(data || []);  // Aseguramos array
//         this.loading.set(false);
//       },
//       error: (err) => {
//         console.error('❌ Error al cargar categorías:', err);
//         this.categorias.set([]);
//         this.loading.set(false);
//       },
//     });
//   }


//   // Función para abrir el diálogo de edición
// categoriaSeleccionada: CategoriaIntegrante | null = null;

// categoriaModalVisible = false;  
// // Abrir modal para crear
// openCreateCategoria() {
//   this.categoriaSeleccionada = null;
//   this.categoriaModalVisible = true;
// }

// // Abrir modal para editar
// openEditCategoria(categoria: CategoriaIntegrante) {
//   this.categoriaSeleccionada = categoria;
//   this.categoriaModalVisible = true;
// }





//   deleteCategoria(id: string) {
//     if (confirm('¿Seguro que deseas eliminar esta categoría?')) {
//       this.categoriasService.delete(id).subscribe(() => {
//         this.categorias.set(this.categorias().filter(c => c.id_categoria !== id));
//       });
//     }
//   }
}
